# Changelog

## 1.1.2 - Unreleased

- restore the paper-trading project from the cleanest archive;
- rename the installed package from `src` to `paper_trading_codex`;
- add deterministic offline tests for data loading, metrics, exchange simulation, portfolio accounting and mocked market data;
- add input validation for impossible financial parameters;
- make Nix environments non-mutating;
- add reproducible packaging through `pyproject.toml`;
- normalize the shipped YAML configuration schema;
- remove the obsolete environment-repair script.
